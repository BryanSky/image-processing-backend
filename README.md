# hackzurich2020-server

> A Django - Vue.js project

## Build Setup

``` bash
# install dependencies
python3 -m pip install -r Scripts\requirements.txt

# serve with hot reload at localhost:5000
python3 main.py

# Front end is in different repository
```

